# 上午进度报告（2026-08-14）

> 时段：约 09:20–13:10（实际耗时远超预期，主要是模型加载与多轮 GPU 验证）
> 环境：RTX 4060 GPU 全程参与；HF 模型需 `HF_HUB_OFFLINE=1`（huggingface.co 被墙）

---

## 一、完成的核心成果

### 1. B1 轻注水漏洞加固 —— 已彻底解决 ✅（本次最重要）

**问题**：MacBERT 升级后，1.5x 轻注水（追加流畅废话）仍能把最终分抬高 +65%。
根因：填充句是"通用网文风"，把整篇向量拉向 H 质心 → **S_repr 0.084→0.288、S_attr 0.255→0.340 同时虚涨**；S_disc 恒定 0.007 已无偏置。

**加固方案**（`configs/default_deploy.yaml`）：
- **S_repr / S_attr 窗口化**：只在前 400 字符窗口上计算（0=全文本向后兼容）
- **α（严苛度）0.2 → 0.5**

**效果**（评分集部署配置下实测）：

| 攻击策略 | 加固前（1.5x 峰值） | 加固后 |
|---|---|---|
| append_filler（末尾注水） | **+65.1%** | **+0.0%** ✅ |
| interleave（段间插入） | **+34.5%** | **+0.0%** ✅ |
| self_repeat（复读原文） | +0.0% | +0.0% ✅ |

- 全 G 注水样本 `Spearman(最终分, 长度) = -0.859`（注水必降分）
- **H 侧代价反而改善**：自然长文(~2x) 从 **-15.7% → -11.1%**（纯 κ 惩罚，无内容位移混淆）
- 30 个单元测试全过（默认 window_chars=0，实验脚本零影响）

**试过不行的路**（已在状态文档记录，避免重复踩坑）：
- 纯提高 α：需 α=0.8 才堵 1.5x，但 H 自然长文 -29% 过度惩罚
- 句级自相似度惩罚：轻注水自相似 0.608 < H 原文 0.633，无法区分

### 2. 跨寄存器解耦验证（真·跨生成器的替代）✅

**真·跨生成器被堵**：ANTHROPIC_API_KEY 无效（401）；DeepSeek v4-flash/v4-pro 均返回空内容（只有 deepseek-chat 能用，与现有 G 同款）。

改为跨**风格寄存器**（非章回 G vs 章回体 GZ）：

| 指标 | 结果 |
|---|---|
| 像人分区分 H vs G（非章回） | **AUC 0.999** |
| 像人分区分 H vs GZ（章回体） | **AUC 1.000** |
| G 内 像人分 vs 质量分 Spearman | **+0.097**（ns，≈0 ✅） |
| GZ 内 像人分 vs 质量分 Spearman | **-0.185**（p=0.066，弱负相关 ⚠️） |

⚠️ **如实报告**：章回寄存器里"像人分越高质量越低"的弱趋势，与 H 内 -0.246 同向。跨寄存器并不完全干净，论文需呈现。

### 3. 评分服务器三套实例已启动 ✅（P0 前置）

- 8770：主评分（盲评，口令 narrate2026）
- 8771：`--label-mode correct`（给真实来源标签）
- 8772：`--label-mode false`（给颠倒来源标签）
- 已用独立数据目录避免状态文件冲突；分离式后台进程，**重启机器后需重新拉起**
- ⚠️ **尚无任何人评分**（这是当前最大阻塞）

### 4. 权限配置 ✅

项目 `.claude/settings.local.json` 已加 `"defaultMode": "bypassPermissions"`，本项目内未来会话无需再逐条审批。

---

## 二、未完成 / 阻塞

| 事项 | 状态 | 原因 |
|---|---|---|
| **人工评分收集（P0）** | 未开始 | 需真实评委访问三套服务器；我无法代评 |
| 真·跨生成器验证 | 被堵 | Anthropic key 无效；DeepSeek v4 空响应。**需用户提供有效 API key** |
| A3 构造证据 | 已评估，搁置 | 3 次失败根因=域漂移；域内方案与来源身份混淆。需重新设计（进训练分布或真人评分数据） |
| 评分服务器持久 | 会话级 | 本会话结束后进程仍在跑（分离式启动），但重启机器需手动拉起 |

---

## 三、下一步建议（按优先级）

1. **人工评分**：转发 `http://172.16.14.82:8770`（口令 narrate2026）给评委，每人 80 条，写评语。这是推动整个实验的关键。
2. **真·跨生成器**：提供有效的 Anthropic API key，我用它生成第二批 G 文本并跑解耦。
3. A3 重构设计（进训练分布的构造样本）或直接用真人评分数据替代。
4. 论文按"解耦性实证"主线组织：正交性 → 区分度分离 → 长度偏置控制 → B1 防刷分 → 跨寄存器。

---

## 四、本次新增/修改文件

- `configs/default_deploy.yaml`（新）：部署加固配置
- `src/narrative_evaluator/metrics/mix_scorer.py`（改）：window_chars 窗口化
- `src/narrative_evaluator/config.py`、`evaluator.py`（改）：window_chars 配置通道
- `scripts/strictness_harden_probe.py`、`probe_window_repr.py`、`probe_self_similarity.py`、`cross_register_probe.py`（新）：诊断/验证脚本
- `scripts/generate_g_claude.py`（改）：已改为 DeepSeek 版（v4-pro 空响应，实际不可用，待 key）
- `PROJECT_STATUS.md`（改）：新增 2.11/2.12，更新待办
